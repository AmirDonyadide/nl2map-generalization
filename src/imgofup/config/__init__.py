"""
imgofup.config

Keep this package initializer minimal to avoid import-time side effects.
Import what you need explicitly:

    from imgofup.config import constants
    from imgofup.config import paths
"""
